module.exports = {
    darkMode: 'class', // or 'media' if you want to follow the system's dark mode
    theme: {
      extend: {
        // Customize your theme here if needed
      },
    },
    variants: {
      extend: {},
    },
    plugins: [],
  }
  